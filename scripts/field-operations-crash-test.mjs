const base=process.argv[2]||'http://127.0.0.1:8850';const origin=new URL(base).origin;const checks=[];const adminPassword='Pikala admin phase nine password!';
class Client{constructor(){this.cookies=new Map();}absorb(response){const values=typeof response.headers.getSetCookie==='function'?response.headers.getSetCookie():[response.headers.get('set-cookie')].filter(Boolean);for(const header of values){const pair=header.split(';')[0];const index=pair.indexOf('=');if(index>0)this.cookies.set(pair.slice(0,index),pair.slice(index+1));}}async request(path,{method='GET',body,redirect='manual'}={}){const headers={accept:'application/json'};if(body!==undefined)headers['content-type']='application/json';if(!['GET','HEAD'].includes(method)){headers['x-pikala-request']='web';headers.origin=origin;}if(this.cookies.size)headers.cookie=[...this.cookies].map(([key,value])=>`${key}=${value}`).join('; ');const response=await fetch(new URL(path,base),{method,headers,body:body===undefined?undefined:JSON.stringify(body),redirect});this.absorb(response);const data=(response.headers.get('content-type')||'').includes('json')?await response.json():null;return{response,data};}}
function check(name,condition,detail=''){if(!condition)throw new Error(`${name}: ${detail||'échec'}`);checks.push(name);}async function expect(client,path,status,options){const value=await client.request(path,options);check(`${options?.method||'GET'} ${path} -> ${status}`,value.response.status===status,`${value.response.status} ${JSON.stringify(value.data)}`);return value;}
async function createUser(label){const client=new Client();const email=`field-${label}-${Date.now()}-${Math.random().toString(16).slice(2)}@example.test`;const password='Pikala field test password!';let value=await client.request('/api/signup',{method:'POST',body:{firstName:'Agent',lastName:label,email,password,locale:'fr'}});check(`inscription ${label}`,value.response.status===202&&value.data.verificationUrl,JSON.stringify(value.data));const verified=await fetch(value.data.verificationUrl,{redirect:'manual'});check(`vérification ${label}`,verified.status===303,String(verified.status));value=await client.request('/api/login',{method:'POST',body:{email,password}});check(`connexion ${label}`,value.response.status===200,JSON.stringify(value.data));return{client,email,password,userId:value.data.user.id};}
async function makeStaff(admin,user,code){const body={email:user.email,employeeCode:code,role:'field_agent',hireDate:new Date().toISOString().slice(0,10),zoneIds:[1],reason:'Création agent pour crash-test terrain',idempotencyKey:crypto.randomUUID(),confirmation:'PIKALA STAFF.CREATE'};await expect(admin,'/api/admin/staff',201,{method:'POST',body});await expect(user.client,'/api/login',200,{method:'POST',body:{email:user.email,password:user.password}});}
const admin=new Client();let value=await admin.request('/api/login',{method:'POST',body:{email:'admin-phase9@example.test',password:adminPassword}});check('connexion admin',value.response.status===200&&value.data.user?.role==='admin',JSON.stringify(value.data));const agent=await createUser('principal');const outsider=await createUser('secondaire');await makeStaff(admin,agent,`FIELD-A-${agent.userId}`);await makeStaff(admin,outsider,`FIELD-B-${outsider.userId}`);
const stationData=await expect(admin,'/api/admin/stations?limit=100',200);const source=stationData.data.items.find(item=>item.public_code==='dev-station-oudayas');const destination=stationData.data.items.find(item=>item.public_code==='dev-station-hassan');check('stations fixtures',source&&destination,JSON.stringify(stationData.data.items));await expect(admin,`/api/admin/stations/${destination.id}`,200,{method:'PATCH',body:{dockCount:8}});
const bikeData=await expect(admin,`/api/admin/bikes?limit=100&stationId=${source.id}`,200);const bike=bikeData.data.items.find(item=>item.public_code==='dev-bike-001');const other=bikeData.data.items.find(item=>item.public_code==='dev-bike-002');check('vélos fixtures',bike&&other,JSON.stringify(bikeData.data.items));
value=await expect(admin,'/api/admin/field/tasks',201,{method:'POST',body:{type:'bike_move',priority:'high',description:'Déplacer un vélo avec preuve de chaque scan',agentId:agent.userId,sourceStationId:source.id,destinationStationId:destination.id,bikeIds:[bike.id],deadline:new Date(Date.now()+3600000).toISOString()}});const taskId=value.data.id;
await expect(outsider.client,`/api/admin/field/tasks/${taskId}`,404);value=await expect(agent.client,'/api/admin/field/tasks?mine=1',200);check('mission visible par agent',value.data.items.some(item=>item.id===taskId),JSON.stringify(value.data.items));
await expect(agent.client,`/api/admin/field/tasks/${taskId}/transition`,409,{method:'POST',body:{status:'completed'}});await expect(agent.client,`/api/admin/field/tasks/${taskId}/transition`,200,{method:'POST',body:{status:'accepted'}});
await expect(agent.client,`/api/admin/field/tasks/${taskId}/scan`,409,{method:'POST',body:{type:'source_station',code:source.public_code,idempotencyKey:crypto.randomUUID()}});await expect(agent.client,`/api/admin/field/tasks/${taskId}/transition`,200,{method:'POST',body:{status:'in_progress'}});
await expect(agent.client,`/api/admin/field/tasks/${taskId}/scan`,409,{method:'POST',body:{type:'source_station',code:destination.public_code,idempotencyKey:crypto.randomUUID()}});await expect(agent.client,`/api/admin/field/tasks/${taskId}/scan`,201,{method:'POST',body:{type:'source_station',code:`pikala://station/${source.public_code}`,idempotencyKey:crypto.randomUUID()}});
await expect(agent.client,`/api/admin/field/tasks/${taskId}/scan`,409,{method:'POST',body:{type:'pickup_bike',code:other.public_code,idempotencyKey:crypto.randomUUID()}});await expect(agent.client,`/api/admin/field/tasks/${taskId}/scan`,201,{method:'POST',body:{type:'pickup_bike',code:`pikala://bike/${bike.public_code}`,idempotencyKey:crypto.randomUUID()}});await expect(agent.client,`/api/admin/field/tasks/${taskId}/scan`,409,{method:'POST',body:{type:'pickup_bike',code:bike.public_code,idempotencyKey:crypto.randomUUID()}});
value=await expect(admin,`/api/admin/bikes/${bike.id}`,200);check('garde réelle enregistrée',value.data.bike.status==='reserved'&&value.data.bike.station_id===null,JSON.stringify(value.data.bike));value=await expect(admin,`/api/admin/stations/${source.id}`,200);check('vélo retiré de la station publique',!value.data.bikes.some(item=>item.id===bike.id),JSON.stringify(value.data.bikes));await expect(agent.client,`/api/admin/field/tasks/${taskId}/transition`,409,{method:'POST',body:{status:'cancelled'}});
await expect(agent.client,`/api/admin/field/tasks/${taskId}/transition`,409,{method:'POST',body:{status:'completed'}});await expect(agent.client,`/api/admin/field/tasks/${taskId}/scan`,201,{method:'POST',body:{type:'destination_station',code:destination.public_code,idempotencyKey:crypto.randomUUID()}});
const sourceDock='dev-dock-oudayas-01';await expect(agent.client,`/api/admin/field/tasks/${taskId}/scan`,409,{method:'POST',body:{type:'deposit_bike',code:sourceDock,dockCode:sourceDock,bikeCode:bike.public_code,idempotencyKey:crypto.randomUUID()}});value=await expect(admin,`/api/admin/stations/${destination.id}`,200);const freeDock=value.data.docks.find(item=>item.status==='available'&&!item.bike_id);check('quai destination libre',Boolean(freeDock),JSON.stringify(value.data.docks));
await expect(agent.client,`/api/admin/field/tasks/${taskId}/scan`,201,{method:'POST',body:{type:'deposit_bike',code:freeDock.public_code,dockCode:freeDock.public_code,bikeCode:bike.public_code,idempotencyKey:crypto.randomUUID()}});await expect(agent.client,`/api/admin/field/tasks/${taskId}/scan`,409,{method:'POST',body:{type:'deposit_bike',code:freeDock.public_code,dockCode:freeDock.public_code,bikeCode:bike.public_code,idempotencyKey:crypto.randomUUID()}});await expect(agent.client,`/api/admin/field/tasks/${taskId}/transition`,200,{method:'POST',body:{status:'completed'}});
value=await expect(admin,`/api/admin/bikes/${bike.id}`,200);check('dépôt réel enregistré',value.data.bike.status==='available'&&Number(value.data.bike.station_id)===Number(destination.id)&&Number(value.data.bike.dock_id)===Number(freeDock.id),JSON.stringify(value.data.bike));value=await expect(agent.client,`/api/admin/field/tasks/${taskId}`,200);check('historique complet',value.data.events.some(item=>item.event_type==='scan.pickup_bike')&&value.data.events.some(item=>item.event_type==='scan.deposit_bike')&&value.data.task.status==='completed',JSON.stringify(value.data));

const suffix=Date.now().toString(36);async function createStation(code,name,bikes){let created=await expect(admin,'/api/admin/stations',201,{method:'POST',body:{publicCode:code,slug:code,name,city:'Rabat',address:'Crash test',latitude:34.02,longitude:-6.84,capacity:3,dockCount:3,isActive:true,openingHours:{}}});for(let index=1;index<=bikes;index+=1)await expect(admin,'/api/admin/bikes',201,{method:'POST',body:{publicCode:`${code}-bike-${index}`,code:`${code.toUpperCase()}-BIKE-${index}`,stationId:created.data.id,status:'available',batteryLevel:100,model:'Test',serialNumber:`${code}-serial-${index}`}});return created.data.id;}
const fullId=await createStation(`field-full-${suffix}`,'Station presque pleine',3);const emptyId=await createStation(`field-empty-${suffix}`,'Station presque vide',0);value=await expect(admin,'/api/admin/field/rebalancing/generate',200,{method:'POST',body:{}});const suggestion=value.data.suggestions.find(item=>Number(item.source_station_id)===Number(fullId)&&Number(item.destination_station_id)===Number(emptyId));check('suggestion D1 réelle',Boolean(suggestion?.suggested_bikes),JSON.stringify(value.data.suggestions));const before=await expect(admin,`/api/admin/bikes?limit=100&stationId=${fullId}`,200);value=await expect(admin,`/api/admin/field/rebalancing/${suggestion.id}`,201,{method:'POST',body:{action:'accept',bikeCount:1,agentId:agent.userId}});check('suggestion convertie en mission',Boolean(value.data.taskId),JSON.stringify(value.data));const after=await expect(admin,`/api/admin/bikes?limit=100&stationId=${fullId}`,200);check('acceptation sans déplacement fictif',before.data.pagination.total===after.data.pagination.total,JSON.stringify({before:before.data.pagination.total,after:after.data.pagination.total}));await expect(admin,`/api/admin/field/rebalancing/${suggestion.id}`,409,{method:'POST',body:{action:'accept',bikeCount:1,agentId:agent.userId}});
console.log(`Crash-test terrain valide : ${checks.length} contrôles.\n${checks.map(item=>`- ${item}`).join('\n')}`);
