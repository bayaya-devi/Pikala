const base=process.argv[2]||'http://127.0.0.1:8830';const origin=new URL(base).origin;const checks=[];const password='Pikala admin phase nine password!';
class Client{constructor(){this.cookies=new Map();}absorb(response){const values=typeof response.headers.getSetCookie==='function'?response.headers.getSetCookie():[response.headers.get('set-cookie')].filter(Boolean);for(const header of values){const pair=header.split(';')[0],index=pair.indexOf('=');if(index>0)this.cookies.set(pair.slice(0,index),pair.slice(index+1));}}async request(path,{method='GET',body,redirect='manual'}={}){const headers={accept:'application/json'};if(body!==undefined)headers['content-type']='application/json';if(!['GET','HEAD'].includes(method)){headers['x-pikala-request']='web';headers.origin=origin;}if(this.cookies.size)headers.cookie=[...this.cookies].map(([key,value])=>`${key}=${value}`).join('; ');const response=await fetch(new URL(path,base),{method,headers,body:body===undefined?undefined:JSON.stringify(body),redirect});this.absorb(response);const data=(response.headers.get('content-type')||'').includes('json')?await response.json():null;return{response,data};}}
function check(name,condition,detail=''){if(!condition)throw new Error(`${name}: ${detail||'echec'}`);checks.push(name);}
function command(action,targetId,reason,data={}){return{action,targetId,reason,data,idempotencyKey:`admin-${crypto.randomUUID()}`,confirmation:`PIKALA ${action.toUpperCase()}`};}
async function createUser(){const client=new Client();const email=`admin-user-${Date.now()}@example.test`;let value=await client.request('/api/signup',{method:'POST',body:{firstName:'Utilisateur',lastName:'AdminTest',email,password:'Pikala ordinary phase nine password!',locale:'fr'}});check('inscription utilisateur normal',value.response.status===202&&value.data.verificationUrl,String(value.response.status));const verified=await fetch(value.data.verificationUrl,{redirect:'manual'});check('verification utilisateur normal',verified.status===303,String(verified.status));value=await client.request('/api/login',{method:'POST',body:{email,password:'Pikala ordinary phase nine password!'}});check('connexion utilisateur normal',value.response.status===200&&value.data.user?.id,String(value.response.status));return{client,email,userId:value.data.user.id};}
const admin=new Client();let value=await admin.request('/api/login',{method:'POST',body:{email:'admin-phase9@example.test',password}});check('connexion admin',value.response.status===200&&value.data.user.role==='admin',`${value.response.status} ${value.data?.user?.role}`);const adminId=value.data.user.id;
const normal=await createUser();value=await normal.client.request('/api/admin/overview');check('API admin refusee utilisateur normal',value.response.status===403&&value.data.code==='FORBIDDEN',String(value.response.status));let page=await normal.client.request('/admin.html');check('page admin refusee utilisateur normal',page.response.status===303&&page.response.headers.get('location')?.includes('dashboard'),String(page.response.status));
value=await admin.request('/api/admin/overview');check('dashboard admin reel',value.response.status===200&&Number.isInteger(value.data.metrics.users)&&value.data.metrics.bikes,String(value.response.status));value=await admin.request(`/api/admin/users/${adminId}`,{method:'PATCH',body:{role:'user',status:'active',statusReason:'test'}});check('auto retrogradation admin refusee',value.response.status===409,String(value.response.status));
const suffix=Date.now();value=await admin.request('/api/admin/stations',{method:'POST',body:{name:'Station Trop Grande',publicCode:`oversized-${suffix}`,slug:`oversized-${suffix}`,city:'Rabat',address:'Test',latitude:34.02,longitude:-6.84,capacity:101,dockCount:101,isActive:true,openingHours:{}}});check('station surdimensionnee refusee',value.response.status===400,String(value.response.status));value=await admin.request('/api/admin/stations',{method:'POST',body:{name:'Station Admin Test',publicCode:`admin-station-${suffix}`,slug:`admin-station-${suffix}`,city:'Rabat',address:'Quartier test',latitude:34.02,longitude:-6.84,capacity:4,dockCount:4,isActive:true,openingHours:{daily:'06:00-23:00'}}});check('creation station',value.response.status===201&&value.data.id,JSON.stringify(value.data));const stationId=value.data.id;
value=await admin.request(`/api/admin/stations/${stationId}`);check('lecture station et quais',value.response.status===200&&value.data.docks.length===4,JSON.stringify(value.data));value=await admin.request(`/api/admin/stations/${stationId}`,{method:'PATCH',body:{name:'Station Admin Modifiee',publicCode:`admin-station-${suffix}`,slug:`admin-station-${suffix}`,city:'Rabat',address:'Quartier test',latitude:34.021,longitude:-6.841,capacity:5,dockCount:5,isActive:true,openingHours:{daily:'07:00-22:00'}}});check('modification station',value.response.status===200,String(value.response.status));
value=await admin.request('/api/admin/bikes',{method:'POST',body:{code:`ADMIN-BIKE-${suffix}`,publicCode:`admin-bike-${suffix}`,stationId,status:'available',batteryLevel:100,model:'Pikala V2',serialNumber:`SERIAL-${suffix}`}});check('creation velo',value.response.status===201&&value.data.id,JSON.stringify(value.data));const bikeId=value.data.id;
value=await admin.request(`/api/admin/bikes/${bikeId}/qr`);check('QR velo unique',value.response.status===200&&value.data.payload===`pikala://bike/admin-bike-${suffix}`,JSON.stringify(value.data));
value=await admin.request('/api/admin/control-center/actions',{method:'POST',body:command('bike.maintenance',bikeId,'Controle complet avant remise en service')});check('passage maintenance',value.response.status===201&&value.data.targetId,JSON.stringify(value.data));const maintenanceId=value.data.targetId;value=await admin.request(`/api/admin/bikes/${bikeId}/maintenance`,{method:'POST',body:{reason:'Double maintenance interdite'}});check('ancienne maintenance refusee',value.response.status===409,String(value.response.status));
value=await admin.request(`/api/admin/workshop/interventions/${maintenanceId}/transition`,{method:'POST',body:{stage:'inspection_required'}});check('maintenance vers inspection',value.response.status===200,String(value.response.status));
const inspectionItems=['brakes','tires','wheels','chain','saddle','lighting','frame','qr','lock','electronics'].map(code=>({code,result:'pass',notes:'Conforme'}));value=await admin.request('/api/admin/workshop/inspections',{method:'POST',body:{maintenanceId,items:inspectionItems,notes:'Inspection administrative complete.'}});check('inspection maintenance',value.response.status===201,String(value.response.status));
for(const transition of [{stage:'diagnosed',diagnosis:'Controle complet, aucun defaut persistant.'},{stage:'maintenance'},{stage:'testing',notes:'Verification et serrage termines.',laborMinutes:15},{stage:'repaired',testResult:'passed',testNotes:'Essai atelier conforme.'},{stage:'available',confirmReturnToService:true}]){value=await admin.request(`/api/admin/workshop/interventions/${maintenanceId}/transition`,{method:'POST',body:transition});check(`maintenance ${transition.stage}`,value.response.status===200,`${value.response.status} ${JSON.stringify(value.data)}`);}
value=await admin.request(`/api/admin/bikes/${bikeId}`);check('velo remis disponible',value.data.bike.status==='available',value.data.bike.status);
value=await normal.client.request('/api/subscriptions/checkout',{method:'POST',body:{plan:'dev-monthly',idempotencyKey:`admin-ride-${suffix}`}});check('abonnement local trajet',value.response.status===201,`${value.response.status} ${value.data?.code||''}`);value=await normal.client.request('/api/rides',{method:'POST',body:{qrPayload:`admin-bike-${suffix}`}});check('creation trajet pour lecture admin',value.response.status===201&&value.data.ride?.id,JSON.stringify(value.data));const rideId=value.data.ride.id;value=await admin.request(`/api/admin/rides/${rideId}`);check('lecture detail trajet admin',value.response.status===200&&value.data.ride.id===rideId,String(value.response.status));value=await normal.client.request(`/api/rides/${rideId}/return`,{method:'POST',body:{dockCode:`admin-station-${suffix}-dock-001`}});check('restitution trajet de test',value.response.status===200,String(value.response.status));
value=await normal.client.request('/api/support',{method:'POST',body:{subject:'Ticket crash-test admin',message:'Demande de test pour le workflow support.'}});check('ticket utilisateur cree',value.response.status===201,String(value.response.status));value=await admin.request('/api/admin/support?search=Ticket%20crash-test');check('liste support admin',value.response.status===200&&value.data.items.length>0,JSON.stringify(value.data));const ticketId=value.data.items[0].id;value=await admin.request(`/api/admin/support/${ticketId}`,{method:'PATCH',body:{status:'resolved',priority:'normal',resolutionNotes:'Demande traitee.'}});check('resolution support',value.response.status===200,String(value.response.status));
value=await admin.request('/api/admin/notifications',{method:'POST',body:{title:'Information de test',body:'Notification locale phase neuf.',type:'service',target:'users',locale:'all',userIds:[normal.userId],reason:'Diffusion ciblée du crash-test administration',confirmation:'PIKALA NOTIFICATION.SEND',idempotencyKey:`notify-${crypto.randomUUID()}`}});check('notification de service',value.response.status===201&&value.data.count===1,JSON.stringify(value.data));
value=await admin.request('/api/admin/settings',{method:'PATCH',body:{key:'ride_monitoring',value:{longRideMinutes:150}}});check('parametre utile modifie',value.response.status===200,String(value.response.status));value=await admin.request('/api/admin/payments',{method:'PATCH',body:{status:'paid'}});check('falsification paiement impossible',value.response.status===404&&value.data.code==='ADMIN_ROUTE_NOT_FOUND',`${value.response.status} ${value.data?.code}`);
value=await admin.request('/api/admin/audit-logs?limit=100');check('audit actions sensibles',value.response.status===200&&value.data.items.some((item)=>item.action==='workshop.transition')&&value.data.items.some((item)=>item.action==='station.create'),JSON.stringify(value.data.items));
value=await admin.request(`/api/admin/stations/${stationId}`,{method:'DELETE',body:{}});check('ancienne fermeture station refusee',value.response.status===409,String(value.response.status));value=await admin.request(`/api/admin/bikes/${bikeId}`,{method:'PATCH',body:{stationId:null}});check('ancien deplacement velo refuse',value.response.status===409,String(value.response.status));value=await admin.request('/api/admin/control-center/actions',{method:'POST',body:command('station.close',stationId,'Fermeture de la station après le crash-test')});check('fermeture station forte',value.response.status===201,String(value.response.status));
console.log(`Crash-test admin valide : ${checks.length} controles.\n${checks.map((item)=>`- ${item}`).join('\n')}`);
